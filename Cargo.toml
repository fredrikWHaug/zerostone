[package]
name = "zerostone"
version = "0.1.0"
edition = "2021"

[dependencies]
libm = "0.2"

[dev-dependencies]
criterion = { version = "0.5", default-features = false }

[[bench]]
name = "circular_buffer"
harness = false
